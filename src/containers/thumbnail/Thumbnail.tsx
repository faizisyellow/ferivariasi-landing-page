import React, { FC } from "react";

interface ThumbnailProps {}

const Thumbnail: FC<ThumbnailProps> = ({}) => {
  return (
    <section className="background-image h-screen w-full"></section>
  );
};

export default Thumbnail;
